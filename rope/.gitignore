node_modules
dist
playtests/bundles/
# 3D props are fetched from a GitHub Release at build time, never committed - see
# "Prop assets" in CLAUDE.md. `bun run assets:fetch` populates this.
public/meshes/
# Raw, un-optimised models as they were downloaded. Local scratch: the shippable
# copy is what `assets:publish` uploads, and a raw is re-downloadable from the
# `source` its manifest entry records.
assets-src/
